export class Commune {
    name: string
    departement: string
    region: string

    constructor(name: string, departement: string, region: string) {
        this.name = name
        this.departement = departement
        this.region = region
    }
}
